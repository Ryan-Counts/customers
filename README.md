# customers

A relatively straight-forward application that takes in customer records from multiple sources and then reconiles them with each other.

To start, you need two terminal windows and need to run these commands:
In the base directory, in one window, use the command "npm run backend" and in the other use "npm run dev".